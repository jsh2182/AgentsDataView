<?xml version="1.0" encoding="utf-8" ?>
<nlog xmlns="http://www.nlog-project.org/schemas/NLog.xsd"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      autoReload="true"
      internalLogLevel="Warn"
      internalLogFile="internal-nlog.log">

  <extensions>
    <add assembly="NLog.Web.AspNetCore"/>
    <add assembly="NLog.Database"/>
    <add assembly="NLog.Appsettings.Standard" />
  </extensions>
  
  <!-- the targets to write to -->
  <targets async="true">
    <target xsi:type="ColoredConsole" name="ColorConsole" layout="${longdate} | ${level} | ${exception:tostring} : ${message} | ${callsite}"/>
    <target xsi:type="Database"  name="DbLog" dbProvider="sqlserver"  connectionString="${appsettings:name=ConnectionStrings.AgentsDataViewConnection}">
      <commandText>
        INSERT INTO dbo.ExceptionLog ( MachineName, Logged, Message, Logger, Exception, HttpAction, CallSite, RequestedURL ) 
        VALUES ( @MachineName, @Logged, @Message,  @Logger, @Exception, @HttpAction, @CallSite, @URL );
      </commandText>

      <parameter name="@MachineName" layout="${machinename}" />
      <parameter name="@Logged" layout="${date}" />
      <parameter name="@Message" layout="${message}" />
      <parameter name="@Logger" layout="${logger}" />
      <parameter name="@Exception" layout="${exception:tostring}" />
      <parameter name="@HttpAction" layout="${aspnet-mvc-action}"/>
      <parameter name="@CallSite" layout="${callsite:fileName=true}"/>
      <parameter name="@URL" layout="${aspnet-request-url}"/>
    </target>
  </targets>

  <!-- rules to map from logger name to target -->
  <rules>
    <logger name="*" minlevel="Info" writeTo="ColorConsole" />
    <logger name="*" minLevel="Error" writeTo="DbLog"/>
  </rules>
</nlog>